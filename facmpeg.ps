%!PS
/FacMPEGFilterFactory 200 dict def
FacMPEGFilterFactory begin

/AdjustYUV { /sat exch def /bri exch def /con exch def } def
/AutoLevels { /strength exch def } def
/AutoContrast { /amount exch def } def
/AutoColor { /amount exch def } def
/BrightnessContrast { /bri exch def /con exch def } def
/ColorBalance { /c exch def /m exch def /y exch def } def
/ColorBoost { /amount exch def } def
/ColorTemperature { /kelvin exch def } def
/Colorize { /h exch def /s exch def } def
/ChannelMixer { /r exch def /g exch def /b exch def } def
/ChromaKey { /h exch def /tol exch def } def
/ChromaBlur { /radius exch def } def
/ChromaSharpen { /amount exch def } def
/ChromaShift { /dx exch def /dy exch def } def
/ContrastAdaptive { /amount exch def } def
/CurveRGB { /points exch def } def
/CurveYUV { /points exch def } def
/DeBand { /strength exch def } def
/DeBlock { /strength exch def } def
/DeNoise { /strength exch def } def
/DeRain { /amount exch def } def
/DeScratch { /amount exch def } def
/DeVignette { /amount exch def } def
/DirectionalBlur { /angle exch def /radius exch def } def
/DisplacementMap { /scale exch def } def
/Dither { /amount exch def } def
/EdgeDetect { /strength exch def } def
/EdgeEnhance { /amount exch def } def
/Emboss { /angle exch def /depth exch def } def
/Equalize { /amount exch def } def
/Exposure { /ev exch def } def
/FilmGrain { /amount exch def } def
/FlipHorizontal { } def
/FlipVertical { } def
/Fog { /amount exch def } def
/Gamma { /g exch def } def
/Glow { /radius exch def /amount exch def } def
/GradientMap { /map exch def } def
/HDRSim { /strength exch def } def
/HeatHaze { /amount exch def } def
/HighPass { /radius exch def } def
/HueSaturation { /h exch def /s exch def } def
/Invert { } def
/InvertLuma { } def
/InvertChroma { } def
/ISOBoost { /amount exch def } def
/Kaleidoscope { /segments exch def } def
/Levels { /black exch def /white exch def } def
/LightRays { /amount exch def } def
/LinearBlur { /radius exch def } def
/LumaKey { /thr exch def } def
/LumaSharpen { /amount exch def } def
/LensBlur { /radius exch def } def
/LensCorrection { /dist exch def } def
/Letterbox { /size exch def } def
/LowPass { /radius exch def } def
/LUTApply { /lut exch def } def
/MagicSharp { /amount exch def } def
/MaskUnsharp { /amount exch def } def
/Median { /radius exch def } def
/Mirror { } def
/Mosaic { /size exch def } def
/MotionBlur { /angle exch def /radius exch def } def
/NoiseAdd { /amount exch def } def
/NoiseRemove { /amount exch def } def
/OilPaint { /radius exch def } def
/Outline { /amount exch def } def
/Pixelate { /size exch def } def
/Posterize { /levels exch def } def
/PushPull { /amount exch def } def
/RadialBlur { /radius exch def } def
/Rain { /amount exch def } def
/ReduceRedEye { /amount exch def } def
/Relief { /amount exch def } def
/ReplaceColor { /h exch def /tol exch def } def
/Resize { /sx exch def /sy exch def } def
/Rotate { /angle exch def } def
/Saturation { /amount exch def } def
/Scanlines { /amount exch def } def
/SelectiveColor { /c exch def /m exch def /y exch def } def
/Sharpen { /amount exch def } def
/SharpenAdaptive { /amount exch def } def
/Shear { /x exch def /y exch def } def
/SkyEnhance { /amount exch def } def
/SmartBlur { /radius exch def } def
/SoftFocus { /amount exch def } def
/Solarize { /thr exch def } def
/SpotHeal { /radius exch def } def
/Stabilize { /amount exch def } def
/Stretch { /x exch def /y exch def } def
/SubtractBG { /amount exch def } def
/Swirl { /angle exch def } def
/TemporalBlur { /radius exch def } def
/Threshold { /thr exch def } def
/TiltShift { /amount exch def } def
/ToneMap { /amount exch def } def
/Transform { /m exch def } def
/Transparency { /alpha exch def } def
/TVNoise { /amount exch def } def
/UnsharpMask { /amount exch def } def
/Vibrance { /amount exch def } def
/Vignette { /amount exch def } def
/Vintage { /amount exch def } def
/Warp { /amount exch def } def
/Wave { /amp exch def /freq exch def } def
/WhiteBalance { /temp exch def } def
/Zoom { /factor exch def } def
/ZoomBlur { /radius exch def } def

end
showpage
