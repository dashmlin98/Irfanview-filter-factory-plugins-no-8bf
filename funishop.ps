%!PS
/FunishopFilterFactory 100 dict def
FunishopFilterFactory begin

/AnamorphicLensFlare { /Intensity exch def /Stretch exch def /Angle exch def } def
/AntiAliasBlur { /Radius exch def } def
/ArtisticPosterize { /Levels exch def } def
/AsciiArt { /Size exch def } def
/BilateralBlur { /Radius exch def /Threshold exch def } def
/BoxBlur { /Radius exch def } def
/BrightnessContrast { /Brightness exch def /Contrast exch def } def
/CameraShakeBlur { /Distance exch def /Angle exch def } def
/Cartoon { /EdgeStrength exch def /ColorLevels exch def } def
/ChannelMixer { /Red exch def /Green exch def /Blue exch def } def
/ChromaticAberration { /Offset exch def } def
/CircularBlur { /Radius exch def } def
/ColorBalance { /CyanRed exch def /MagentaGreen exch def /YellowBlue exch def } def
/ColorInvert { } def
/Colorize { /Hue exch def /Saturation exch def } def
/ComicInk { /Thickness exch def } def
/Crystallize { /CellSize exch def } def
/Curves { /ControlPoints exch def } def
/Denoise { /Strength exch def } def
/DepthOfField { /Focus exch def /Blur exch def } def
/DisplacementMap { /Scale exch def } def
/Dither { /Amount exch def } def
/DropShadow { /Distance exch def /Opacity exch def } def
/EdgeDetect { /Strength exch def } def
/Emboss { /Height exch def /Angle exch def } def
/Exposure { /EV exch def } def
/FilmGrain { /Amount exch def } def
/Fisheye { /Distortion exch def } def
/Glow { /Radius exch def /Intensity exch def } def
/GaussianBlur { /Radius exch def } def
/GradientMap { /Map exch def } def
/Grayscale { } def
/HighPass { /Radius exch def } def
/HDRTone { /Strength exch def } def
/HueSaturation { /Hue exch def /Saturation exch def /Lightness exch def } def
/Infrared { /Strength exch def } def
/InvertAlpha { } def
/Kaleidoscope { /Segments exch def } def
/LensCorrection { /Distortion exch def } def
/LightRays { /Intensity exch def /Angle exch def } def
/LineArt { /Threshold exch def } def
/Liquify { /Pressure exch def } def
/Magnify { /Zoom exch def } def
/Mirror { /Axis exch def } def
/MotionBlur { /Distance exch def /Angle exch def } def
/NeonGlow { /Radius exch def /Color exch def } def
/NoiseAdd { /Amount exch def } def
/OilPaint { /Brush exch def } def
/OldPhoto { /Fade exch def } def
/Outline { /Thickness exch def } def
/PaintDaubs { /Size exch def } def
/PanZoom { /Pan exch def /Zoom exch def } def
/PerspectiveWarp { /Strength exch def } def
/PhotoFilter { /Color exch def /Density exch def } def
/Pixelate { /Size exch def } def
/Plasma { /Turbulence exch def } def
/PosterEdges { /Thickness exch def } def
/Posterize { /Levels exch def } def
/RadialBlur { /Amount exch def } def
/Relief { /Depth exch def } def
/Ripple { /Amplitude exch def /Frequency exch def } def
/Sharpen { /Amount exch def } def
/Sketch { /Detail exch def } def
/SmartBlur { /Radius exch def /Threshold exch def } def
/Smudge { /Strength exch def } def
/Solarize { /Threshold exch def } def
/Spherize { /Amount exch def } def
/SpinBlur { /Angle exch def } def
/StainedGlass { /CellSize exch def } def
/Stamp { /Threshold exch def } def
/SurfaceBlur { /Radius exch def } def
/Swirl { /Angle exch def } def
/Threshold { /Level exch def } def
/TiltShift { /Blur exch def /Focus exch def } def
/ToneMapping { /Strength exch def } def
/Twirl { /Angle exch def } def
/UnsharpMask { /Amount exch def /Radius exch def } def
/Vignette { /Amount exch def } def
/VintageFade { /Amount exch def } def
/Wave { /Amplitude exch def /Wavelength exch def } def
/Watercolor { /Brush exch def } def
/Wind { /Strength exch def } def
/Woodcut { /Detail exch def } def
/ZoomBlur { /Amount exch def } def

end
